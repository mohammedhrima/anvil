# A function whose body throws must not leave its parameters bound afterwards.
# Reachable only because the REPL now survives errors; see ScopedBind.
fn f(who) {
    boom()
}
fn peek() {
    output("LEAKED who=" + who)
}
loop
